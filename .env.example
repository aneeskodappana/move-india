# Synthetic demo database only. Never use a production or government-system database.
DATABASE_URL=postgresql://demo_user:demo_password@demo-host.example/vandi_demo?sslmode=require

# Fixed authentication code used only by the clearly labelled DEV MODE flow.
DEV_OTP_CODE=123456

# Replace with a long random value in deployed environments.
SESSION_SECRET=replace-with-at-least-32-random-characters
